这份完整的 **Coding Plan（编码实施方案）** 针对您调整后的技术栈，摒弃了繁琐的配置，将所有开发步骤划分为 **5 个清晰的阶段（Milestones）**。

每个阶段都配备了 **具体开发步骤**、**核心逻辑提示**、**自我测试标准（Verify）** 以及 **避坑指南**，帮助您稳扎稳打地完成这个大作业。

---

### 📅 第一阶段：画布搭建与“无库”静态渲染（第 1 天）
**目标**：在不使用任何复杂图形库的前提下，实现深色画布、节点定位和带箭头连线的渲染。

#### 1.1 开发步骤
1.  **文件准备**：在 `src` 文件夹下保留 `main.jsx`、`App.jsx` 和 `App.css`，其余无用文件全部删除，清空默认样式。
2.  **样式铺设**：将前面给出的 `App.css` 内容完整复制到 `App.css` 中。
3.  **状态定义**：在 `App.jsx` 中初始化两个节点、一个 Root、一条边的静态假数据（写在 `useState` 的默认值里），用于界面开发调试。
4.  **SVG 连线绘制**：
    *   在画布中放置 `svg` 节点并占满全屏。
    *   定义好 SVG `<marker>` 箭头模型。
    *   使用 `edges.map` 循环渲染 `<line>`，线的起点和终点坐标读取 `nodes` 中对应节点的 `x, y`。

#### 1.2 避坑指南
*   **SVG 遮挡问题**：必须给 `svg` 标签加上 `pointer-events: none;`。否则 `svg` 会像一块透明玻璃一样盖在画布上，导致你**无法用鼠标点击或拖拽下方的节点**。

#### 1.3 本阶段测试标准 (Verify)
*   [ ] 打开浏览器，能看到深色背景的画布和左侧控制台。
*   [ ] 页面上正确渲染出绿色的 Root 圆圈、一个灰色的 Obj_A 圆圈。
*   [ ] 有一条深灰色的连线从 Root 顺畅地指向 Obj_A，且指向圆圈边缘的位置带有清晰的箭头。

---

### 📅 第二阶段：节点的手动拖拽与物理联动（第 2 天）
**目标**：让静态的节点“活”起来，实现鼠标拖拽节点、连线实时跟着拉伸弯曲的丝滑动效。

#### 2.1 开发步骤
1.  **确定拖拽节点**：在 React 中声明状态 `const [draggedNodeId, setDraggedNodeId] = useState(null);`。
2.  **鼠标按下事件**：在节点的 `div` 上绑定 `onMouseDown`，触发时设置 `draggedNodeId` 为当前节点 ID。
3.  **鼠标移动事件**：在**画布的容器 `div`** 上绑定 `onMouseMove`。如果 `draggedNodeId` 不为空：
    *   通过 `canvasRef.current.getBoundingClientRect()` 获取画布的绝对屏幕位置。
    *   计算出鼠标相对于画布左上角的相对坐标 `x` 和 `y`。
    *   用 `setNodes` 的函数式更新更新目标节点的坐标。
4.  **鼠标抬起事件**：在画布容器上绑定 `onMouseUp` 和 `onMouseLeave`，将 `draggedNodeId` 设回 `null`。

#### 2.2 核心逻辑：防止点击和拖拽冲突
```javascript
// 在节点的 onMouseDown 中，必须阻止冒泡
const handleMouseDown = (nodeId, e) => {
  e.stopPropagation(); // 阻止事件向上冒泡，避免触发画布的其他点击事件
  setDraggedNodeId(nodeId);
};
```

#### 2.3 本阶段测试标准 (Verify)
*   [ ] 按住绿色 Root 节点并用鼠标拖动，圆圈能在画布内自由移动。
*   [ ] 拖动时，指向其他节点的连线箭头会像橡皮筋一样实时拉伸、改变方向。
*   [ ] 鼠标松开后，圆圈停留在松开的位置。

---

### 📅 第三阶段：画布动态编辑与右键删除（第 3 天）
**目标**：允许用户自由往画布里添加内存块、连接引用关系，以及清除这些关系。

#### 3.1 开发步骤
1.  **新增节点**：实现 `addNode` 函数，点击左侧按钮时，生成一个唯一的随机 ID（如 `Date.now()`），坐标设在画布中心附近，推入 `nodes` 数组。
2.  **创建引用（连线模式）**：
    *   声明 `const [linkingSourceId, setLinkingSourceId] = useState(null)`。
    *   用户点击“建立连线”按钮，进入连线状态。
    *   点击第一个节点：设 `linkingSourceId` 为该节点。
    *   点击第二个节点：在 `edges` 状态中推入新边，并重置 `linkingSourceId`。
3.  **删除节点（右键快捷操作，极简好用）**：
    *   在节点 `div` 上绑定 `onContextMenu`（右键点击）。
    *   在回调中执行 `e.preventDefault()` 阻止系统默认菜单，然后将该节点从 `nodes` 过滤掉，同时在 `edges` 中过滤掉所有与该节点相关的连线。

#### 3.2 本阶段测试标准 (Verify)
*   [ ] 点击“+ 新增对象”，画布中能生成新的灰色圆圈，可以被拖动。
*   [ ] 点击“连线”按钮，依次点击 Root 和新对象，能成功拉出一条指向新对象的单向线。
*   [ ] 在任意节点上右键，该节点和所有指向它（或由它引出）的连线瞬间消失。

---

### 📅 第四阶段：核心算法一：标记-清除与时间调度（第 4 天）
**目标**：实现 Mark-and-Sweep 垃圾回收算法，并用“慢动作”动画展示它的工作机制。

#### 4.1 开发步骤
1.  **编写延时器**：在文件顶部写好 `const sleep = (ms) => new Promise(resolve => setTimeout(resolve, ms));`。
2.  **实现标记阶段 (Mark)**：
    *   进入函数时，设置 `isSimulating(true)` 锁定左侧所有编辑按钮。
    *   初始化队列（Queue）和已访问集合（Visited）。
    *   从所有 `isRoot === true` 的节点出发跑 BFS。
    *   在 `while` 循环里，每弹出一个节点：
        *   修改该节点状态为 `'marked'`。
        *   **执行 `await sleep(600)` 挂起函数**，让 React 渲染引擎有时间更新 DOM 展现黄色高亮。
        *   继续遍历它的出度邻居。
3.  **实现清扫阶段 (Sweep)**：
    *   遍历结束，将没有高亮的节点（死亡对象）状态设为 `'sweeping'`。
    *   **执行 `await sleep(800)`**，等待 CSS 的 `scale(0.2)` 和 `opacity: 0` 过渡效果播完。
    *   用 `filter` 真正把这些节点从 `nodes` 数组中移除，解除锁定 `isSimulating(false)`。

#### 4.2 避坑指南：异步更新陷阱
在异步延时过程中，如果直接用 `setNodes(nodes.map(...))` 可能会拿到旧的状态。**必须始终使用 React 状态的函数式更新**：
```javascript
// 推荐写法
setNodes(prev => prev.map(n => n.id === currId ? { ...n, state: 'marked' } : n));
```

#### 4.3 本阶段测试标准 (Verify)
*   [ ] 点击“运行：标记-清除”后，高亮（黄色）顺着 Root 节点一步一步、慢动作向后传染。
*   [ ] 扫描结束后，没有被连线连到的孤立节点会缓缓变小、淡出，最后彻底从网页上消失。
*   [ ] 运行过程中，控制面板的所有编辑按钮均变为灰色（不可点状态），避免用户在扫描时捣乱。

---

### 📅 第五阶段：核心算法二：引用计数与预设场景演示（第 5 天）
**目标**：实现级联垃圾回收，并通过“经典循环引用”场景对比两者的优劣，完成终期测试。

#### 5.1 开发步骤
1.  **引用计数角标显示**：
    *   在 `useEffect` 中监听 `edges` 的改变。
    *   每次连线变动，重新计算每个非 Root 节点的入度并赋给 `refCount`，渲染在圆圈右上角红色角标中。
2.  **级联回收算法逻辑**：
    *   用一个 `while(hasDeleted)` 循环扫描。
    *   每次找出所有非 Root 且 `refCount === 0` 的节点。
    *   如果找到了，将它们状态设为 `'sweeping'`，等待 800ms 后从 `nodes` 和 `edges` 中清除，然后进行下一轮扫描，直到没有节点可以被删除为止。
3.  **加载预设（大作业加分项）**：
    *   实现“一键加载循环引用场景”按钮。该场景创建：`Root -> A`，以及孤立的环 `B ⇄ C`。

#### 5.2 终期验收与演示流程（如何惊艳导论课老师）
你可以拿着运行好的项目向老师进行如下演示，这是最有力、最能证明学术价值的成果：
1.  点击控制面板的“**加载：循环引用缺陷场景**”。
2.  向老师指出：“*此时 B 和 C 与根节点（Root）完全断开。然而，由于它们互相指向，其引用计数（rc）依然保留为 1。*”
3.  点击运行“**引用计数回收**”：你会看到系统**毫无反应**，Obj_B 和 Obj_C 依然残留在内存中。向老师解释这就是引用计数垃圾回收算法的局限性——无法解决循环引用导致的内存泄露。
4.  接着，点击运行“**标记-清除回收**”：你会看到黄色高亮从 Root 传到了 Obj_A。扫描结束后，环状的 Obj_B 和 Obj_C 因为不可达，在渐隐动画中**被完美回收**。证明了标记-清除算法的优越性。